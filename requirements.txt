pywebview>=5.0
requests>=2.31
qrcode>=7.4
pynput>=1.7
# pygame-ce (community edition): drop-in for pygame (`import pygame` unchanged),
# but ships prebuilt wheels for new Python versions much sooner. Classic pygame
# has no cp314 wheel, so it tries to compile from source on Python 3.14 and the
# build fails. Both are optional — the app disables joystick PTT if absent.
pygame-ce>=2.5
# SimConnect: pure-python wrapper around MSFS's SimConnect.dll. Installs on any
# OS (pure python); the DLL only loads at runtime on Windows when MSFS runs.
# Imported lazily in msfs_source so non-Windows / no-sim machines are unaffected.
SimConnect>=0.4.26
